# AWS Glue ETL Job Dependencies
# These are pre-installed in AWS Glue environment

# Core dependencies (pre-installed in Glue)
pyspark>=3.3.0
boto3>=1.26.0
awsglue>=4.0.0

# Testing dependencies (for local development and CI/CD)
pytest>=7.4.0
hypothesis>=6.82.0
pytest-spark>=0.6.0

# Additional dependencies if needed
# pandas>=1.5.0  # Available in Glue Python Shell jobs
# numpy>=1.23.0  # Available in Glue Python Shell jobs
